<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Declares that the companion does not need watch hardware. This alone does
         NOT keep it off watches - it only makes the APK eligible on both form
         factors. Watches get the wear APK because Play's form-factor targeting
         prefers the one declaring android.hardware.type.watch as required.
         The gap: a Wear OS 2 watch (API 26-29) is below the wear module's
         minSdk 30, so it can still match this APK. Exclude those in the Play
         Console device rules if it ever matters. -->
    <uses-feature
        android:name="android.hardware.type.watch"
        android:required="false" />

    <!-- See the comment in the wear manifest: no automatic Google Drive backup of
         third-party OAuth tokens or workout data. -->
    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
