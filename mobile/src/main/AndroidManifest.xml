<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- The companion must NOT install on watches: Google Play picks between the
         two APKs of the shared applicationId by form factor, and the watch one
         declares android.hardware.type.watch as required. -->
    <uses-feature
        android:name="android.hardware.type.watch"
        android:required="false" />

    <!-- See the comment in the wear manifest: no automatic Google Drive backup of
         third-party OAuth tokens or workout data. -->
    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
