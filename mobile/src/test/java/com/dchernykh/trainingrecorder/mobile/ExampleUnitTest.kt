package com.dchernykh.trainingrecorder.mobile

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * Placeholder unit test that keeps the JVM test source set wired up, so the
 * `testDebugUnitTest` CI gate has something to run from the first commit.
 */
class ExampleUnitTest {
    @Test
    fun additionIsCorrect() {
        assertEquals(4, 2 + 2)
    }
}
