package com.dchernykh.trainingrecorder.core

/**
 * Marks the shared module. Replaced by real domain types as they land; it exists
 * so the module compiles and its test source set has something to link against
 * from the first commit.
 */
internal object Core
